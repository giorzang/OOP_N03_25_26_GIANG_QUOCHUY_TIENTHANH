<%- include('../includes/head.ejs') %>
<body class="custom-bg"> 
<%- include('../includes/navigation.ejs') %>

<main>
    <div class="container" style="margin-top: 50px; margin-bottom: 50px;">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="admin-page-title mb-0"><i class="fas fa-boxes me-2"></i> Quản lý Sản phẩm</h2>
            
            <a href="/admin/add-product" class="btn btn-admin-primary">
                <i class="fas fa-plus me-1"></i> Thêm sản phẩm mới
            </a>
        </div>

        <% if (prods.length > 0) { %>
            <div class="table-responsive overflow-hidden">
                <table class="table admin-table align-middle mb-0">
                    <thead> 
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Tên sản phẩm</th>
                            <th scope="col">Giá</th>
                            <th scope="col">Phân loại</th>
                            <th scope="col" class="text-center">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let product of prods) { %>
                            <tr>
                                <td><%= product.id %></td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <img src="<%= product.imageUrl %>" alt="<%= product.name %>" style="width: 45px; height: 45px; object-fit: cover; margin-right: 10px; border-radius: 4px;">
                                        <strong><%= product.name %></strong>
                                    </div>
                                </td>
                                <td><span class="product-price small"><%= parseFloat(product.price).toLocaleString('vi-VN') %> VND</span></td>
                                <td><%= product.category_name %></td>
                                <td class="text-center">
                                    <a href="/admin/edit-product/<%= product.id %>" class="btn btn-action-edit me-2">
                                        <i class="fas fa-edit"></i> Sửa
                                    </a>
                                    
                                    <form action="/admin/delete-product" method="POST" class="d-inline">
                                        <input type="hidden" name="productId" value="<%= product.id %>">
                                        <button type="submit" class="btn btn-action-delete">
                                            <i class="fas fa-trash-alt"></i> Xóa
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="alert alert-warning text-center" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i> Chưa có sản phẩm nào. Hãy thêm sản phẩm mới!
            </div>
        <% } %>
    </div>
</main>

<div style="height: 100px;"></div>

<%- include('../includes/end.ejs') %>